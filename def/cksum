#!/cw
path /bin:/usr/bin:/sbin:/usr/sbin:<env>
# this may be used for other purposes than coloring to stdin.
nopipe
base cyan
match cyan+:default /
ifarg --h
match cyan+:default -
ifarg-else
token cyan+:none 0 32
token none:default 1 32
